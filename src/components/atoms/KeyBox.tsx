const KeyBox = ({}: KeyBoxProps) => {
  return (
    <div className="col-span-1 flex items-center justify-center rounded-lg bg-gainsboro py-4 px-6">
      <span className="invisible font-body text-4xl font-bold text-white">
        K
      </span>
    </div>
  );
};

type KeyBoxProps = {};

export default KeyBox;
